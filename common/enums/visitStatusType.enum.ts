export enum VisitStatusType {
	ACCEPTED = 'ACCEPTED',
	REJECTED = 'REJECTED',
}

export enum ColorStatusVisit {
	ACCEPTED = 'is-success',
	REJECTED = 'is-success',
}

export enum VisitStatusTranslateType {
	ACCEPTED = 'ACEPTADO',
	REJECTED = 'RECHAZADO',
}
